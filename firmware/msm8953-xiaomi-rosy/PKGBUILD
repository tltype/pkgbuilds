_mode=cross
pkgname=firmware-msm8953-xiaomi-rosy
pkgver=0.1
pkgrel=1
_arches=specific
arch=(aarch64)
license=(custom:Proprietary)
options=(!strip)
_commit=b5b5f886070a2dce41a9cd9d545eba50dcdfa10a
source=(
    "git+https://github.com/comradeFreeman/firmware-xiaomi-rosy#commit=$_commit"
)
sha256sums=(
    SKIP
)
_fwdir="/usr/lib/firmware/qcom/msm8953/xiaomi/rosy"

package() {
    cd "$srcdir/firmware-xiaomi-rosy"
    install -Dm644 a506_zap.mdt -t "$pkgdir/$_fwdir"
    install -Dm644 a506_zap.b02 -t "$pkgdir/$_fwdir"
}
